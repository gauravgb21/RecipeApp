import React from 'react';

const Filter = props => {
    return(
        <div className={"filter-cont-bg"}>
            <div className={'filter-cont__main'}>
                <header className={"filter-cont__main"}>
                    <span className={"filter-cont__main"}></span>
                </header>
            </div>
        </div>
    );
}

export default Filter;